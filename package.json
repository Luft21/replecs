{
    "private": true,
    "type": "module",
    "scripts": {
        "build:frontend": "vite build",
        "build": "npm run build:frontend && php artisan config:cache && php artisan route:cache && php artisan event:cache && php artisan view:cache",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.1.7",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.1.7",
        "vite": "^6.2.4"
    },
    "dependencies": {
        "tailwind-scrollbar": "^4.0.2"
    }
}
